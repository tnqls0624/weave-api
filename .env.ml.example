# ML Training 환경 변수 예시
# 실제 사용 시 .env 파일로 복사하여 수정하세요

# MongoDB 연결
MONGODB_URI=mongodb://root:1234@mongodb:27017/weave?authSource=admin

# ML 재학습 설정
ML_RETRAIN_DAYS=30                # 최근 N일간의 데이터 사용
ML_AUTO_DEPLOY=true                # 자동 배포 여부

# 실행 모드
ML_RUN_MODE=cron                   # cron: 자동 스케줄링, once: 일회성 실행

# Cron 스케줄 (cron 표현식)
# 예시:
#   0 2 * * 0    - 매주 일요일 새벽 2시 (권장)
#   0 2 * * *    - 매일 새벽 2시
#   0 2 1 * *    - 매월 1일 새벽 2시
#   */30 * * * * - 30분마다 (테스트용)
ML_CRON_SCHEDULE=0 2 * * 0

# 초기 학습 데이터 최소 요구사항
# 최소 100개의 레이블된 샘플 권장
# 피싱:정상 = 1:1 권장
